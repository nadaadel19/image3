on:
  push:
    branches:
      - master
     

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "login to docker"
        run: login docker --username nadaadel19 --password dckr_pat_HSEfUFd1fcUFGrYRvq8gCdpc6Hk 
